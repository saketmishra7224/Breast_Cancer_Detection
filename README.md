# Project: Breast Cancer Detection

This folder contains a scaffold for a FastAPI backend and a minimal frontend skeleton.

Folders:
- `backend/` — FastAPI app
- `frontend/` — React/Vite skeleton

Run the backend (example):

```powershell
cd backend
pip install -r requirements.txt
uvicorn app.main:app --reload --port 8000
```
